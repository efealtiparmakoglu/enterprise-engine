apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-engine-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-engine.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-engine-service
            port:
              number: 80
